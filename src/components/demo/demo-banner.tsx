export function DemoBanner() {
  return (
    <div className="bg-amber-50 px-4 py-2 text-center text-xs text-amber-800">
      UI preview only — no backend is connected. Accounts, listings, and checkout are not real; nothing
      you enter here is saved.
    </div>
  );
}
